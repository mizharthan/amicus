const Tabs = () => {
  return (
    <div className="tabs">
      <button className="active">Ranking</button>
      <button>Schedule</button>
      <button>Network</button>
    </div>
  );
};

export default Tabs;
